# bundle patch for dsh-plugin-guard: inserts its row into the profile roster.
# Applied as a profile bundle layer (the `dsh.bundle.patch` manifest field);
# install with `dsh plugin --profile web add file:<this directory>`.
- insert:
    - id: ui-plugin-guard
      name: 'dsh-plugin-guard'
